'use client';

import React from 'react';
import RefFinanceSwapCard from './RefFinanceSwapCard';

/**
 * Main Swap Widget Component
 * Now uses Ref Finance enhanced swap card with all advanced features
 */
export const RheaSwapWidget: React.FC = () => {
  return <RefFinanceSwapCard />;
};

export default RheaSwapWidget;
